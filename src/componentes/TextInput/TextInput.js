


const TextInput = ({label, placeHolder}) => {
    return (
        <div>
            <label>{label}</label>
            <input placeholder={placeHolder} />
        </div>
    )
}

export default TextInput